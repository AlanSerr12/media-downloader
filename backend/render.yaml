services:
  - type: web
    name: media-downloader-api
    env: node
    buildCommand: "./render-build.sh"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production